namespace Compiler

module Processor =
    let buildAst (topLevel:Word) =
        let rec compileWord word =
            ()
        and compileMacro macro =
            ()
        
        compileWord topLevel